<?php
$appName = "Task Manager";
$taskTitle = "Вивчити PHP";
$taskTimeEstimate = 10;
?>

    <header>
        <h1><?= $appName; ?></h1>
        <ul>
            <li> <?= $taskTitle; ?></li>
            <li> <?= $taskTimeEstimate ;?> </li>
        </ul>
    </header>

1) Що таке динамічна типізація і як вона працює в PHP? Динамічні типізація це коли одна й та сама змінна може змінювати свій тип протягом її використання у коді, через призначення їй нового значення 
2) Як правильно розпочати і закрити блок з PHP-кодом всередині HTML-документа? Правильно почати та закривати стандартно, як мову PHP <?php *код*?>
3) В чому різниця між записами <?php echo $var; ?> та <?= $var ?>? У довжині записи коду, проте їхня функція однакова
4) Які основні (скалярні) типи даних підтримує PHP? Наведіть приклади. Основними типами даних є: цілі числа(наприклад, 10, 109, 5883), числа з плаваючою комою(наприклад, 23.54, 120.2245432), рядки(просто довільний текст), логічні змінні(True/False).
5) Що буде виведено в браузері, якщо користувач відкриє файл .html, всередині якого буде вбудовано <?php echo "Test"; ?> без налаштувань сервера? Поясніть чому. Буде пуста сторінка, тому що PHP серверна мова програмування, тому він браузер її не знає і сприймає як щось неправильне або невідомий код HTML, тому ігнорує та не відображає на екрані.